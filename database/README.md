# Database Layer

This folder will contain database-related code and configuration (ORM models, migrations, and connection utilities).

Suggested next step: decide on a database (e.g. SQLite + Prisma, PostgreSQL, or MongoDB) so we can scaffold the actual schema and integration.
